version: 2

builds:
  - id: exporter
    main: ./cmd/exporter
    binary: technitium-dns-exporter
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format_overrides:
      - goos: windows
        format: zip
    files:
      - config/config.yaml

checksum:
  name_template: 'checksums.txt'

dockers:
  - image_templates:
      - 'ghcr.io/{{ tolower .Env.GITHUB_REPOSITORY }}:{{ .Tag }}'
      - 'ghcr.io/{{ tolower .Env.GITHUB_REPOSITORY }}:{{ .Major }}.{{ .Minor }}'
      - 'ghcr.io/{{ tolower .Env.GITHUB_REPOSITORY }}:{{ .Major }}'
      - 'ghcr.io/{{ tolower .Env.GITHUB_REPOSITORY }}:latest'
    dockerfile: deploy/Dockerfile
    use: buildx
    goarch: amd64

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^style:'
      - '^ci:'
      - '^Merge'
